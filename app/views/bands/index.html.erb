<div class="flex flex-wrap justify-center p-10 lg:m-10">
  <h1 class="lg:text-4xl h2 theme-text" >尋找最適合您的樂團</h1>
  <div class="flex flex-wrap justify-center w-full">
    <%= search_form_for @search_band do |f| %>
      <div class="flex items-center justify-center my-10">
        <div>
          <%= f.search_field :name_or_styles_name_cont, class:'p-3 mx-3 rounded-xl joband-ring-offset' %>
        </div>
        <button class="flex justify-center mx-2 w-14 h-14">
          <%= render "shared/search_btn" %>
        </button>
      </div>
    <% end %>
  </div>
      <div class="flex h-[90px] items-center pt-16">
    <% Style.all.each do |style| %>
      <div class="lg:w-[50px] lg:h-[50px] lg:hover:w-[100px] lg:hover:h-[100px] lg:hover:text-3xl lg:m-5
                  w-[50px] h-[50px] h4 m-1 duration-300 ">
        <%= render "shared/style_search", style: style.name %>
      </div>
    <% end %>
  </div>
</div>
<div class="flex flex-wrap justify-center w-full">
  <div class="lg:my-10">
    <%= render @bands %>
  </div>
</div>