<h1 class="text-2xl text-white">找樂團</h1>

  <ul>
      <% @bands.each do |band| %>
      <div>
        <%= render "shared/avatar", model: band  %>
      </div>
    <li>
      <%= link_to band.name, band_path(band)  %>
      <%= render 'style', band: band %>
      <%= link_to '編輯', edit_band_path(band) %>
    </li>
  </ul>


      
