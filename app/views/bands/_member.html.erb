<p class="font-bold text-lg">成員</p><br>
<%= link_to profile_path(member.user.profile), action: 'go' do %>
  <%= member.role %>
  <%= member.user.name %>
  <div class="h-20 w-20 relative">
   <%= render "shared/avatar", model: member.user.profile %>
  </div>
<% end %>
