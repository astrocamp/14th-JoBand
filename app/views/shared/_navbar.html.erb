<nav class="flex justify-between px-4 py-5 bg-gray-900 text-white">
  <ul class="flex gap-2">
    <%= link_to 'JoBand', root_path %>
    <li><a href="#">找樂手</a></li>
    <li><a href="#">找樂團</a></li>
    <li><a href="#">找活動</a></li>
  </ul>

  <ul class="flex gap-2">
    <% if user_signed_in? %>
      <li><%= current_user.email %></li>
      <li><%= link_to "登出", destroy_user_session_path,
                             data: { turbo_method: 'delete' } %></li>
    <% else %>
      <li><%= link_to "登入", new_user_session_path %></li>
      <li><%= link_to "註冊", new_user_registration_path %></li>
    <% end %>
  </ul>
</nav>
