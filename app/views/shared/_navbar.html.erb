<nav class="flex justify-between text-white navbar bg-base-100 self-start">
  <div class="flex-1 self-start gap-5">
    <%= link_to 'JoBand', root_path, class: "logo-icon" %>
    <a href="#" class="btn btn-ghost normal-case text-xl">找樂手</a>
    <a href="#" class="btn btn-ghost normal-case text-xl">找樂團</a>
    <a href="#" class="btn btn-ghost normal-case text-xl">找活動</a>
  </div>
  <div class="flex">
    <ul class="flex gap-5">
      <% if user_signed_in? %>
        <li>
          <div class="dropdown dropdown-hover mb-32">
            <label class="m-1 btn">
              <%= link_to current_user.email, "#"%>
            </label>
            <ul class="p-2 shadow menu dropdown-content z-[1] bg-base-100 rounded-box w-52">
              <li><%= link_to "個人檔案", "#", class: "nav-link" %></li>
              <li><%= link_to "登出", destroy_user_session_path,
                              data: { turbo_method: 'delete' }, class: "nav-link" %></li>
            </ul>
          </div>
        </li>
        
      <% else %>
        <li><%= link_to "登入", new_user_session_path, class: "btn btn-ghost normal-case text-xl" %></li>
        <li><%= link_to "註冊", new_user_registration_path, class: "btn btn-ghost normal-case text-xl" %></li>
      <% end %>
    </ul>
  </div>
</nav>
